{
  "buildCommand": "cd ../.. && pnpm install --frozen-lockfile && cd apps/server && pnpm run build",
  "outputDirectory": "dist",
  "functions": {
    "dist/index.js": {
      "runtime": "nodejs20.x"
    }
  }
}
